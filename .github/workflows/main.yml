  on:
    push:
      branches:
        - master



  jobs:
    build:
     runs-on: ubuntu-latest

     steps:
       - uses: actions/checkout@v3
         

       - name: 'login to docker hub'
         run: docker login --username abolkhair --password dckr_pat_VeXvip0awbc97kUFYbOvRmUVblg
          -name: "building docker image"
          run: docker build -t abolkhair/aa .
            -name: "pushing to docker hub"
             run: docker push abolkhair/aa 
