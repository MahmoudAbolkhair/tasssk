  on:
    push:
      branches:
        - master



  jobs:
    build:
     runs-on: ubuntu-latest

     steps:
       - uses: actions/checkout@3
         run: pwd

       - name: 'testing branch'
         run: echo mahmoud
