  on:
    push:
      branches:
        - master



  jobs:
    build:
     runs-on: ubuntu-latest

     steps:
       - uses: actions/checkout@v3
         run: pwd

       - name: 'login to docker hub'
         run: docker login --username abolkhair --passworddckr_pat_VeXvip0awbc97kUFYbOvRmUVblg
