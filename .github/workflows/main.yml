  on:
    push:
      branches:
        - master



  jobs:
    build:
     runs-on: ubuntu-latest

     steps:
       - name: 'testing git hub action'
         run: pwd

       - name: 'testing branch'
         run: echo mahmoud
